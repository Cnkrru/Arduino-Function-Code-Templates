//这段代码是对机械臂恢复初始状态功能的优化（cursor使用结构体优化后）

// 定义舵机复位参数表
struct ArmResetCmd {
  char id;   // 'a' 'b' 'c' 'd'
  int  deg;  // 目标角度
  int  ti;   // 每步延时(ms)
};

ArmResetCmd armResetTable[] = {
  {'a', 90, 10},
  {'b', 90, 10},
  {'c', 90, 10},
  {'d', 90, 10},
};
const int ARM_RESET_COUNT = sizeof(armResetTable) / sizeof(armResetTable[0]);

// 带参数的 ArmServer，保留你原有的安全校验/动作逻辑
void ArmServer(char bianhao, int jiaodu1, int ti) {
  // 按你的 ArmServer 逻辑实现：
  // 1) 读当前角度 jiaodu0
  // 2) 判断范围（用 a_armMAX/MIN 等）
  // 3) for 循环平滑转动，delay(ti);
  //    记得把固定的 delay(10) 换成 delay(ti)
}

// 复位时批量调用
void resetAllArms() {
  for (int i = 0; i < ARM_RESET_COUNT; i++) {
    ArmServer(armResetTable[i].id,
              armResetTable[i].deg,
              armResetTable[i].ti);
  }
}